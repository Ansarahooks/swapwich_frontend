class Meals {
    constructor(obj)    {
        this.name        = obj.name
        this.desc        = obj.description
        this.img_url     = obj.img_url
        this.category    = obj.category
        this.hot         = obj.hot?
        this.user_id     = obj.user_id
    }
}